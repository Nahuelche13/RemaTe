using System.IO;
using System.Reflection;
/*
using GetText;

namespace RemaTe.GNOME.Helpers;


public static class Gettext {
    private static readonly ICatalog Catalog = new Catalog("remate", Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location));

    public static string _(string text) {
        return Catalog.GetString(text);
    }

    public static string _(string text, params object[] args) {
        return Catalog.GetString(text, args);
    }

    public static string N(string text, string pluralText, long n) {
        return Catalog.GetPluralString(text, pluralText, n);
    }

    public static string N(string text, string pluralText, long n, params object[] args) {
        return Catalog.GetPluralString(text, pluralText, n, args);
    }

    public static string P(string context, string text) {
        return Catalog.GetParticularString(context, text);
    }

    public static string P(string context, string text, params object[] args) {
        return Catalog.GetParticularString(context, text, args);
    }

    public static string Pn(string context, string text, string pluralText, long n) {
        return Catalog.GetParticularPluralString(context, text, pluralText, n);
    }

    public static string Pn(string context, string text, string pluralText, long n, params object[] args) {
        return Catalog.GetParticularPluralString(context, text, pluralText, n, args);
    }
}
*/
