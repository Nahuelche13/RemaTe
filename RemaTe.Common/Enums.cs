namespace RemaTe.Common.Enums;
public enum Errors {
    Ok,
    NotFound,
    Unauthorized,
    Duplicated,
    Unknown
}
public enum Permissions {
    Cliente = 1,
    Vendedor = 2,
    Empleado = 3,
    Admin = 4,
}
